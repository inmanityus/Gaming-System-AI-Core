{
  "commands": [
    "cd /home/ubuntu",
    "echo === Installing PyTorch with CUDA 12.8 ===",
    "pip3 install torch torchvision torchaudio --index-url https://download.pytorch.org/whl/cu121",
    "echo === Verifying PyTorch ===",
    "python3 -c 'import torch; print(\"PyTorch:\", torch.__version__); print(\"CUDA:\", torch.cuda.is_available()); print(\"GPU:\", torch.cuda.get_device_name(0))'",
    "echo === Installing training dependencies ===",
    "pip3 install transformers peft bitsandbytes datasets accelerate safetensors",
    "echo === Verifying all dependencies ===",
    "python3 -c 'import transformers, peft, bitsandbytes, datasets, accelerate; print(\"All dependencies installed successfully\")'",
    "echo === Installation complete ==="
  ]
}

