# Core dependencies
boto3==1.34.20
botocore>=1.34.20
psutil==5.9.6
pynvml==11.5.0
requests==2.32.3

# Optional: systemd-python (has native fallback if unavailable)
# Install via OS package if available: apt-get install python3-systemd
# systemd-python==235; sys_platform == 'linux'

